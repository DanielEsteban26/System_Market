<Window x:Class="System_Market.Views.ReportesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reportes"
        Height="640" Width="1000"
        MinHeight="520" MinWidth="860"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MainBgLocal}"
        FontFamily="Segoe UI"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <!-- Paleta local -->
        <LinearGradientBrush x:Key="MainBgLocal" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0D47A1" Offset="0"/>
            <GradientStop Color="#1E88E5" Offset="1"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="CardBg" Color="#0F2430"/>
        <SolidColorBrush x:Key="CardBgLight" Color="#14323F"/>
        <SolidColorBrush x:Key="AccentTeal" Color="#19C1D3"/>
        <SolidColorBrush x:Key="HeaderText" Color="#E8F6F9"/>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBgLight}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Color="#22000000"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Botón base con estado disabled atenuado -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#7B1FA2"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="6,6,0,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="6" ShadowDepth="2" Color="#22000000"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="RenderTransform">
                                    <Setter.Value><ScaleTransform ScaleX="1.03" ScaleY="1.03"/></Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="RenderTransform">
                                    <Setter.Value><ScaleTransform ScaleX="0.98" ScaleY="0.98"/></Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.45"/>
                                <Setter TargetName="Bd" Property="Effect" Value="{x:Null}"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#0288D1"/>
        </Style>
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#EF5350"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="CardTitle">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="{StaticResource HeaderText}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="FieldLabel">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#CDE8EE"/>
            <Setter Property="Margin" Value="0,6,0,6"/>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="4,4,4,18" VerticalAlignment="Center">
            <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="22" Foreground="{StaticResource HeaderText}" Margin="0,0,10,0"/>
            <TextBlock Text="Reportes" FontSize="26" FontWeight="Bold" Foreground="{StaticResource HeaderText}"/>
        </StackPanel>

        <!-- Cards area -->
        <UniformGrid Grid.Row="1" Columns="3" Rows="2" VerticalAlignment="Top" HorizontalAlignment="Stretch">
            <!-- Productos -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="Productos"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <CheckBox x:Name="chkIncluirAgotados" Foreground="{StaticResource HeaderText}" VerticalAlignment="Center" Margin="0,4,12,0">Incluir agotados</CheckBox>
                        <Button x:Name="btnExportarProductosExcel" Style="{StaticResource AccentButton}" Click="BtnExportarProductosExcel_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Margin="0,0,8,0"/>
                                <TextBlock Text="Exportar EXCEL"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <Button x:Name="btnExportarProductosExcel2" Style="{StaticResource PrimaryButton}" Click="BtnExportarProductosExcel_Click">
                            <StackPanel Orientation="Horizontal"><TextBlock FontFamily="Segoe MDL2 Assets" Text="" Margin="0,0,8,0"/><TextBlock Text="Exportar EXCEL"/></StackPanel>
                        </Button>
                        <Button x:Name="btnExportarProductosGrafico" Style="{StaticResource AccentButton}" Click="BtnExportarProductosGrafico_Click">
                            <StackPanel Orientation="Horizontal"><TextBlock FontFamily="Segoe MDL2 Assets" Text="" Margin="0,0,8,0"/><TextBlock Text="Exportar gráfico"/></StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Historial de Compras -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="Historial de Compras"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Filtrar por fechas:"/>
                    <StackPanel Orientation="Horizontal">
                        <DatePicker x:Name="dpDesdeCompras" Width="140" Margin="0,0,8,0"/>
                        <DatePicker x:Name="dpHastaCompras" Width="140"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <Button x:Name="btnExportarComprasExcel" Style="{StaticResource AccentButton}" Click="BtnExportarComprasExcel_Click">
                            <StackPanel Orientation="Horizontal"><TextBlock FontFamily="Segoe MDL2 Assets" Text="" Margin="0,0,8,0"/><TextBlock Text="Exportar EXCEL"/></StackPanel>
                        </Button>
                        <Button x:Name="btnExportarComprasPdf" Style="{StaticResource PrimaryButton}" Click="BtnExportarComprasPdf_Click">
                            <StackPanel Orientation="Horizontal"><TextBlock FontFamily="Segoe MDL2 Assets" Text="" Margin="0,0,8,0"/><TextBlock Text="Exportar PDF"/></StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Proveedores -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="Proveedores"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Filtrar por proveedor"/>
                    <ComboBox x:Name="cbProveedores" Width="220" Margin="0,0,0,8"/>
                    <Button x:Name="btnExportarProveedoresExcel" Style="{StaticResource AccentButton}" Click="BtnExportarProveedoresExcel_Click">
                        <StackPanel Orientation="Horizontal"><TextBlock FontFamily="Segoe MDL2 Assets" Text="" Margin="0,0,8,0"/><TextBlock Text="Exportar EXCEL"/></StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Historial de Ventas -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="Historial de Ventas"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Filtrar por fechas:"/>
                    <StackPanel Orientation="Horizontal">
                        <DatePicker x:Name="dpDesdeVentas" Width="140" Margin="0,0,8,0"/>
                        <DatePicker x:Name="dpHastaVentas" Width="140"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <Button x:Name="btnExportarVentasExcel" Style="{StaticResource AccentButton}" Click="BtnExportarVentasExcel_Click">
                            <StackPanel Orientation="Horizontal"><TextBlock FontFamily="Segoe MDL2 Assets" Text="" Margin="0,0,8,0"/><TextBlock Text="Exportar EXCEL"/></StackPanel>
                        </Button>
                        <Button x:Name="btnExportarVentasPdf" Style="{StaticResource PrimaryButton}" Click="BtnExportarVentasPdf_Click">
                            <StackPanel Orientation="Horizontal"><TextBlock FontFamily="Segoe MDL2 Assets" Text="" Margin="0,0,8,0"/><TextBlock Text="Exportar PDF"/></StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Categorías -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="Categorías"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Tipo a analizar:"/>
                    <ComboBox x:Name="cbTipoAnalizar" Width="160" Margin="0,0,0,8">
                        <ComboBoxItem>Categoria</ComboBoxItem>
                        <ComboBoxItem>Producto</ComboBoxItem>
                    </ComboBox>
                    <Button x:Name="btnExportarCategoriasGrafico" Style="{StaticResource AccentButton}" Click="BtnExportarCategoriasGrafico_Click">
                        <StackPanel Orientation="Horizontal"><TextBlock FontFamily="Segoe MDL2 Assets" Text="" Margin="0,0,8,0"/><TextBlock Text="Exportar gráfico"/></StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Footer: Totales y acciones -->
        <Grid Grid.Row="2" Margin="6,18,6,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Total:" FontSize="16" Foreground="{StaticResource HeaderText}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock x:Name="txtTotal" Text="—" FontSize="16" Foreground="{StaticResource HeaderText}" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnHistorial" Style="{StaticResource PrimaryButton}" Click="BtnHistorial_Click" Margin="0,0,12,0">
                    <StackPanel Orientation="Horizontal"><TextBlock FontFamily="Segoe MDL2 Assets" Text="" Margin="0,0,8,0"/><TextBlock Text="HISTORIAL"/></StackPanel>
                </Button>
                <Button x:Name="btnCerrar" Style="{StaticResource DangerButton}" Click="BtnCerrar_Click">
                    <StackPanel Orientation="Horizontal"><TextBlock FontFamily="Segoe MDL2 Assets" Text="" Margin="0,0,8,0"/><TextBlock Text="CERRAR"/></StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>