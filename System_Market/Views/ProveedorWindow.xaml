<Window x:Class="System_Market.Views.ProveedorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:sys="clr-namespace:System;assembly=System.Runtime"
        Title="Gestión de Proveedores"
        Height="560" Width="1100"
        MinHeight="480" MinWidth="820"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MainBgLocal}"
        FontFamily="Segoe UI"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <!-- Fondo similar al resto de vistas -->
        <LinearGradientBrush x:Key="MainBgLocal" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0B2E3A" Offset="0"/>
            <GradientStop Color="#117A8A" Offset="1"/>
        </LinearGradientBrush>

        <!-- Colores y métricas -->
        <SolidColorBrush x:Key="CardBg" Color="#0D1B22"/>
        <SolidColorBrush x:Key="CardBgLight" Color="#12242C"/>
        <SolidColorBrush x:Key="AccentTeal" Color="#19C1D3"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#0288D1"/>
        <SolidColorBrush x:Key="DangerColor" Color="#D9534F"/>
        <SolidColorBrush x:Key="HeaderText" Color="#E8F6F9"/>
        <SolidColorBrush x:Key="MutedText" Color="#8EA6AB"/>
        <SolidColorBrush x:Key="RowDivider" Color="#2E3A3F"/>
        <sys:Double x:Key="CtlHeight">36</sys:Double>

        <!-- Sombra ligera reutilizable -->
        <DropShadowEffect x:Key="BtnShadow" Color="#22000000" BlurRadius="8" ShadowDepth="2" Opacity="0.8"/>

        <!-- Estilo base: botón "pill" con template para CornerRadius -->
        <Style x:Key="PillButtonBase" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="Foreground" Value="{StaticResource HeaderText}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Background" Value="{StaticResource AccentTeal}"/>
            <Setter Property="BorderBrush" Value="#0A000000"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="18"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.95"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.9"/>
                                <Setter TargetName="Bd" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.45"/>
                                <Setter TargetName="Bd" Property="Effect" Value="{x:Null}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Variantes de color basadas en PillButtonBase -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource PillButtonBase}">
            <Setter Property="Background" Value="{StaticResource AccentTeal}"/>
            <Setter Property="materialDesign:ShadowAssist.ShadowDepth" Value="Depth2"/>
            <Setter Property="Effect" Value="{StaticResource BtnShadow}"/>
        </Style>
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource PillButtonBase}">
            <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Effect" Value="{StaticResource BtnShadow}"/>
        </Style>
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PillButtonBase}">
            <Setter Property="Background" Value="{StaticResource DangerColor}"/>
            <Setter Property="Effect" Value="{StaticResource BtnShadow}"/>
        </Style>
        <Style x:Key="NeutralButton" TargetType="Button" BasedOn="{StaticResource PillButtonBase}">
            <Setter Property="Background" Value="#36474C"/>
            <Setter Property="Effect" Value="{StaticResource BtnShadow}"/>
        </Style>

        <!-- Encabezados igual que ProductoWindow -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Foreground" Value="{StaticResource AccentTeal}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- Estilo base del DataGrid (igual a ProductoWindow) -->
        <Style x:Key="NiceDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="RowBackground" Value="#0F000000"/>
            <Setter Property="AlternatingRowBackground" Value="#14000000"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#1FFFFFFF"/>
            <Setter Property="VerticalGridLinesBrush" Value="#1FFFFFFF"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- Estilos locales de celdas/filas (hover/selección coherentes) -->
        <Style x:Key="GridCellStyle" TargetType="DataGridCell">
            <Setter Property="Foreground" Value="{StaticResource HeaderText}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#081617"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource CardBg}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GridRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="MinHeight" Value="36"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#9E9E9E"/>
                    <Setter Property="Foreground" Value="{StaticResource CardBg}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DarkTextBox: template redondeado con borde, hover y foco -->
        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="Foreground" Value="{StaticResource HeaderText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource RowDivider}"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentTeal}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentTeal}"/>
                                <Setter TargetName="Bd" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#22000000" BlurRadius="6" ShadowDepth="2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- header -->
            <RowDefinition Height="Auto"/>   <!-- form card -->
            <RowDefinition Height="*"/>      <!-- list card -->
            <RowDefinition Height="Auto"/>   <!-- actions -->
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12" VerticalAlignment="Center">
            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE716;" FontSize="24" Foreground="{StaticResource AccentTeal}" Margin="0,0,12,0"/>
            <StackPanel>
                <TextBlock Text="Proveedores" FontSize="22" FontWeight="Bold" Foreground="{StaticResource HeaderText}"/>
                <TextBlock Text="Gestión de proveedores" Foreground="{StaticResource MutedText}" FontSize="12"/>
            </StackPanel>
        </StackPanel>

        <!-- Formulario en tarjeta -->
        <Border Grid.Row="1" CornerRadius="10" Background="{StaticResource CardBgLight}" Padding="14" Margin="0,0,0,12" materialDesign:ShadowAssist.ShadowDepth="Depth1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Nombre" Foreground="{StaticResource AccentTeal}" Margin="2,0,0,6" FontSize="12"/>
                    <TextBox x:Name="txtNombre" Style="{StaticResource DarkTextBox}" materialDesign:HintAssist.Hint="Nombre del proveedor"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="14,0,14,0">
                    <TextBlock Text="RUC" Foreground="{StaticResource AccentTeal}" Margin="2,0,0,6" FontSize="12"/>
                    <TextBox x:Name="txtRUC" Style="{StaticResource DarkTextBox}" materialDesign:HintAssist.Hint="RUC o identificación"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Teléfono" Foreground="{StaticResource AccentTeal}" Margin="2,0,0,6" FontSize="12"/>
                    <TextBox x:Name="txtTelefono" Style="{StaticResource DarkTextBox}" materialDesign:HintAssist.Hint="Teléfono de contacto"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Lista en tarjeta grande -->
        <Border Grid.Row="2" CornerRadius="10" Background="{StaticResource CardBg}" Padding="12" materialDesign:ShadowAssist.ShadowDepth="Depth2">
            <Grid>
                <TextBlock Text="Lista de proveedores" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource HeaderText}" Margin="6,0,0,8"/>
                <DataGrid x:Name="dgProveedores"
                          Margin="0,32,0,0"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          SelectionChanged="dgProveedores_SelectionChanged"
                          Style="{StaticResource NiceDataGrid}"
                          HeadersVisibility="Column"
                          AlternationCount="2"
                          CanUserResizeRows="False"
                          Background="Transparent"
                          BorderThickness="0"
                          Foreground="{StaticResource HeaderText}"
                          GridLinesVisibility="All"
                          RowHeight="44"
                          materialDesign:DataGridAssist.CellPadding="10,8,10,8"
                          materialDesign:DataGridAssist.ColumnHeaderPadding="10,8,10,8">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridCell" BasedOn="{StaticResource GridCellStyle}"/>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource GridRowStyle}"/>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                        <DataGridTextColumn Header="RUC" Binding="{Binding RUC}" Width="200"/>
                        <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="180"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Botones agrupados en contenedor con borde -->
        <Border Grid.Row="3" BorderBrush="{StaticResource RowDivider}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,12,0,0" Background="Transparent" >
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Button x:Name="btnAgregar" Style="{StaticResource PrimaryButton}" Click="btnAgregar_Click">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE710;" Margin="0,0,8,0"/>
                        <TextBlock Text="Agregar" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>

                <Button x:Name="btnActualizar" Style="{StaticResource AccentButton}" Click="btnActualizar_Click" IsEnabled="False">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8A7;" Margin="0,0,8,0"/>
                        <TextBlock Text="Actualizar" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>

                <Button x:Name="btnEliminar" Style="{StaticResource DangerButton}" Click="btnEliminar_Click" IsEnabled="False">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE74D;" Margin="0,0,8,0"/>
                        <TextBlock Text="Eliminar" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>

                <Button x:Name="btnRefrescar" Style="{StaticResource NeutralButton}" Click="btnRefrescar_Click">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE72C;" Margin="0,0,8,0"/>
                        <TextBlock Text="Refrescar" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>